# Frontend (Vite) environment variables. Copy to .env.local for local dev.
# On Vercel, set this to your public API base URL, e.g. https://mini-ctf-backend.onrender.com
# If unset in production the app falls back to http://localhost:8080 and OAuth sign-in breaks.
VITE_OAUTH_BASE_URL=http://localhost:8080
